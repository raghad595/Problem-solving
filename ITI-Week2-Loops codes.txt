#include <iostream>
using namespace std;

int main() {

	/*EX 1 : Create a program print all numbers
			 from 1 to 10
	*/

	/*
	int x = 0;
	for (x = 1; x <= 10; x++)
	{
		cout << x << endl;
	}
	*/

	//======================================

	/*EX2: Create a program taking a number from
		   user then print all numbers
		   from 1 to this number
	*/

	/*
	cout << "Please enter a number :\n";
	int x;
	cin >> x;

	for (int i = 1; i <= x; i++)
	{
		cout << i << endl;
	}
	*/

	//======================================

	/*Quiz 1: Create a program to print all
				even numbers to 10

	*/

	/*
	for (int i = 1; i <= 10; i++)
	{
		if (i % 2 == 0) {
			cout << i << endl;
		}
	}
	*/

	//======================================
	/* Ex 3  : Write a program that Print all
				numbers that can be divided by
				3 or 4 from 1 up to 10 .
	*/

	/*
	
	for(int i=1; i<10 ;i++){
		if(i%3==0 || i%4==0){
			cout << i << endl;
		}
	}
	*/
	//======================================

	/* Quiz 2 : Create a program to print how many number
			are divisible by 3 or 4 from 1 to 10 .
	*/

	/*
	int count=0;
	for(int i=1; i <= 10 ;i++){
		if(i%3==0 || i%4==0){
			count++;
		}
	}
	cout<<"Count = "<<count<<endl;
	*/

	//======================================
	/* Ex 4  : Write a program that Print
				all small letters.

	*/

	/*
	for (char i = 'a'; i <= 'z'; i++) {
		cout << i << " ";
	}
	*/


	//======================================
	/*
	EX 5:
	Create a program
	to print sum of numbers from 1 to 10.
	*/

	/*
	int sum = 0;
	for (int i = 0; i <= 10; i++) {
		sum += i;
	}
	cout << "Sum = " << sum;
	*/

	//======================================
	/*
	Quiz 3:
	Create a program to print factorial of number.
	*/
	/*
	int num, fact = 1;
	cin >> num;
	for (int i = 1; i <= num; i++) {
		fact *= i;
	}
	cout << "Fact of " << num << "= " << fact << endl;
	*/

	//======================================
	/*
	EX 6 :Write a program that Take an integer 
		then print all divisor of this number 
	For example user insert 8 then print 1 2 4 8
	another example user insert 9 then print 1 3 9

	*/

	/*
	int x;
	cin >> x;

	for (int i = 1; i <= x; i++) {
		if (x % i == 0) {
			cout << i << " ";
		}
	}
	*/

	//======================================
	/*
	Quiz 4:
	Write a program that Print the max of 10 integers.
	*/

	/*
	int num = 0 , max = INT_MIN;
	for (int i = 1; i <= 10; i++) {
		cin >> num;
		if (num > max) {
			max = num;
		}
	}
	cout << "Max = " << max << endl;
	*/

	//======================================
	/*
	EX 7: Create a program to take a number
			and print prime or not prime.
	*/
	/*
	int x;
	cin >> x;
	int count = 0;
	for (int i = 1; i <= x; i++) {
		if (x % i == 0) {
			count++;
		}
	}
	if (count == 2)
		cout << "Prime" << endl;
	else
		cout << "Not prime" << endl;

	*/

	//======================================

	//Ex 8: print number digits :

	/*
	int num;
	cin >> num;

	int count = 0;

	for (; num > 0;) {
		count++;
		num /= 10;
	}
	cout << count << endl;
	*/
	  //======================================

	//Quiz 5: Reverse a number :
	/*
	int num;
	cin >> num;

	int rev = 0;

	for (; num > 0;) {
		rev = rev*10 + num % 10  ;
		num /= 10;
	}
	cout << rev << endl;
	*/

	//======================================

	

