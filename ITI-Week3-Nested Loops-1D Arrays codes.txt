#include <iostream>
using namespace std;

//=============== Nested Loops ===========
/*
/* EX 1 :   create a program to display from
				Multiplication table 1 
				to Multiplication table 10
	*/

	/*
	for (int num1{ 1 }; num1 <= 10; ++num1) {

		for (int num2{ 1 }; num2 <= 10; ++num2) {
			cout << num1 << " * " << num2
				 << " = " << num1 * num2 << endl;
		}

		cout << "-----------" << endl;
	}

	cout << endl;
	*/
	//======================================

	/* EX 2 :   Create a program to take height 
				from user and print a square *

					****
					****
					****
					****
	*/

	/*
	cout << "Please enter a square length :\n ";
	int x;
	cin >> x;

	for (int i = 1; i <= x ; ++i) {

		for (int j = 1; j <= x ; ++j)
		{
			cout << "*" ;
		}

		cout << endl;
	}
	*/

	//======================================

	/* Quiz 1 :   create a program to take height from user
				  and print a square angled tringle .

					*
					**
					***
					****

	*/

	/*
	cout << "Please enter a tringle height :\n ";
	int x;
	cin >> x;

	for (int i = 1; i <= x ; ++i) {

		for (int j = 1; j <= i ; ++j)
		{
			cout << "*" ;
		}
		cout << endl;
	}
	*/

	//======================================

	/*
	EX 3 :  Write a program that Take from 
			user width and height from user 
			then print empty rectangle .

	*/

	/*
	cout << "Please enter w and h of rec :\n ";
	int w , h;
	cin >> w >> h;

	for (int i = 1; i <= h; ++i) {
		for (int j = 1; j <= w; ++j){
			if (i == 1 || i == h || j == 1 || j == w) {
				cout << "*";
			}
			else {
				cout << " ";
			}
		}
		cout << endl;
	}
	*/

	//======================================

	/*
	EX 4 :
			Write a program that Print all 
			prime numbers between two integers .

	*/

	/*
	int x , y;
	cin >> x >> y;
	for (int i = x; i <= y; i++) {
		int count = 0;
		for (int j = 1; j <= i; j++) {
			if (i % j == 0) {
				count++;
			}
		}
		if (count == 2)
			cout << i << " = Prime" << endl;
	}
	*/

}
*/

//=============== 1D Arrays ==============
int main()
{
	/*EX1 :
		Write a program that 
		Print summation of an array.
	*/

	/*
	int arr[10];
	int sum = 0;
	for (int i = 0; i < 10; i++){
		cin >> arr[i];
		sum += arr[i];
	}
	cout << "Sum = " <<  sum << endl;
	*/

	//=======================================

	/*Quiz 1 :
		Write a program that 
		Print positive numbers 
		of an array.
	*/
	
	/*
	int arr[10];
	for (int i = 0; i < 10; i++){
		cin >> arr[i];
	}

	cout << "\nPOS Numbers : \n";
	for (int i = 0; i < 10; i++){
		if (arr[i] > 0) {
			cout << arr[i]<< endl;
		}
	}
	*/

	//=======================================

	/*Ex 2 :
		Write a program that
		Replace all negative 
		numbers with zero.
	*/

	/*
	int arr[10];
	for (int i = 0; i < 10; i++){
		cin >> arr[i];
		if (arr[i] < 0) {
			arr[i] = 0;
		}
	}

	cout << "\nNumbers after replacing: \n";
	for (int i = 0; i < 10; i++){
		cout << arr[i]<< endl;
	}
	*/

	//=======================================
	/*Quiz 2 :
		Write a program that 
		Get the max of an array.
	*/

	/*
	int arr[10];
	int max = INT_MIN;
	for (int i = 0; i < 10; i++){
		cin >> arr[i];
		if (arr[i] > max) {
			max = arr[i];
		}
	}
	cout << "Max = " <<  max << endl;
	*/

	//=======================================
	/*
	Ex 3: Create a program taking array
		  from user and then print beside
		  each number prime or not prime

		  inputs  :  1 5 9 7 10
		  output :  1 = not prime
					 5 = prime
					 9 = not prime
					 7 = prime
					10 = not prime
	*/

	/*
	cout << "Enter 5 Numbers : \n";
	int arr[5];
	for (int i = 0; i < 5; i++) {
		cin >> arr[i];
	}

	for(int j=0 ; j<5 ;j++){
		int count = 0 ;
		for (int i = 1; i <= arr[j]; i++) {

			if (arr[j] % i == 0) {
				count++;
			}
		}
		if (count == 2) {
			cout << arr[j] << " = Prime\n";
		}
		else{
			cout << arr[j] << " = Not Prime\n";
		}
		*/


	//=====================================
	/*
	Ex 4: Write a program that 
	Search for value in array.

	*/
	
    /*
	int arr[5];

	cout << "Enter array of 5 elements :\n";

	for (int i = 0; i < 5; i++) {
		cin >> arr[i];
	}

	cout << "Enter a value to find :\n";
	int value;
	cin >> value;

	int isFound = -1;
	for (int i = 0; i < 5; i++) {
		if (value == arr[i]) {
			isFound = i;
			break;
		}
	}

	if (isFound != -1) {
		cout << value << " is founded at index = " << isFound << endl;
	}
	else {
		cout << value << " is not founded\n" << endl;
	}
	
	*/
	//=====================================
	/*
	Ex 5: Insert value at specific
		 index in array then shift
		 other numbers
	*/

	/*

	int arr[6];

	cout << "Enter array of 5 elements :\n";

	for (int i = 0; i < 5; i++) {
		cin >> arr[i];
	}

	cout << "Enter an index bet 0&4 to insert :\n";
	int ind;
	cin >> ind;

	cout << "Enter a value to be inerted :\n";
	int value;
	cin >> value;

	for (int i = 4; i >= ind; i--) {
		arr[i + 1] = arr[i];
	}

	arr[ind] = value;

	for (int i = 0; i < 5; i++) {
		cout << arr[i] <<" ";
	}
	*/
	//=======================================

	/*Quiz 3 :
		Create a program taking an array
		from user and print it reversed .
			For example 2,4,8,6,11
			Print 11,6,8,4,2
	*/

	using another Array:
	==========================
	/*
	int arr[5];
	int temp[5];
	for (int i = 0; i < 5; i++){
		cin >> arr[i];
	}

	for (int i = 4; i >= 0; i--){
		temp[4 - i] = arr[i];
	}

	for (int i = 0; i < 5; i++){
		cout << temp[i] << endl;
	}
	*/

	//or using single variable:
	//==========================

	/*
	int arr[5], temp;
	cout << "Enter 5 numbers" << endl;
	for (int i = 0; i < 5; i++) {
	    cin >> arr[i];
	}
	for (int i = 0; i < 2; i++) {
	    temp = arr[i];
	    arr[i] = arr[4 - i];
	    arr[4 - i] = temp;
	}
	for (int i = 0; i < 5; i++) {
	    cout << arr[i] << " ";
	}
	*/
	